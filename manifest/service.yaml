apiVersion: v1
kind: Service
metadata:
  name: prometheus-provider
  namespace: prometheus-provider
spec:
  ports:
  - port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    run: prometheus-provider
  sessionAffinity: None
